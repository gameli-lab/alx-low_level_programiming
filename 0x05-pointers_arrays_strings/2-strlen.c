#include "main.h"
#include <string.h>

/**
 *_strlen - finds ths lenght of the string
 *
 *@*s: variable whose lenght is to be found
 *
 *Return: returns the lenght of the string
 */

int _strlen(char *s)
{
	return(strlen(s));
}
